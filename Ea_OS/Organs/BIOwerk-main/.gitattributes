# Prevent CRLF/LF issues across platforms
* text=auto

# Shell scripts should always use LF
*.sh text eol=lf

# Python files
*.py text eol=lf

# Docker files
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# Windows scripts use CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf

# Markdown and docs
*.md text eol=lf

# Model files (binary, no conversion)
*.gguf binary
*.bin binary
*.safetensors binary
*.ggml binary
