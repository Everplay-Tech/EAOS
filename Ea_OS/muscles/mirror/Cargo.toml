[package]
name = "ea-mirror"
version = "0.1.0"
edition = "2021"
authors = ["Ea Foundation"]
description = "Mirror - Simulation & Consequence Engine"

[dependencies]
muscle-contract = { path = "../../muscle-contract", default-features = false }

[lib]
crate-type = ["rlib"]
